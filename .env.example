# Database (Mokshagna / Poornesh) — confirm shared vs. separate DBs with them 
DATABASE_URL=postgresql://username:password@localhost:5432/sih26102_db

# ML Engine (Kousic) 
ML_ENGINE_URL=http://localhost:8080

# Auth / JWT (Poornesh — backend-case-management)
JWT_SECRET_KEY=replace-with-a-long-random-string-never-commit-real-value
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=60

# Frontend (Chandana) — confirm Vite vs Create React App prefix with her
VITE_API_BASE_URL=http://localhost:8000

# Cloud (Mohith)
GCP_PROJECT_ID=your-gcp-project-id
GCP_REGION=asia-south1